import VisualThumbnailPresentation from 'components/MediaThumbnail/visualThumbnailPresentation'

// Only presentation layers are present in MediaThumbnail component,
// Skipping the default structure of container, data and presentation layers.
export default {
  Visual: VisualThumbnailPresentation,
}
